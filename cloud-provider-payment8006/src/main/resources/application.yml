server:
  port: 8006
spring:
  application:
    name: cloud-provider-payment
  cloud:
    consul:
      host: 49.234.99.130
      port: 8500
      discovery:
        service-name: ${spring.application.name}